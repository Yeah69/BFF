<controls:MetroWindow x:Class="BFF.MainWindow"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:usercontrols="clr-namespace:BFF.WPFStuff.UserControls"
        xmlns:converters="clr-namespace:BFF.WPFStuff.Converters"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:validationRules="clr-namespace:BFF.WPFStuff.ValidationRules"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:behaviors="clr-namespace:BFF.WPFStuff.Behaviors"
        lex:LocalizeDictionary.DesignCulture="en-US"
        lex:ResxLocalizationProvider.DefaultAssembly="BFF"
        lex:ResxLocalizationProvider.DefaultDictionary="Resources"
        Title="{Binding Title}" Height="700" Width="1300" MinHeight="700" MinWidth="1300" ShowIconOnTitleBar="True" Icon="Icons/logo_quadratic.png" 
        ResizeMode="CanResizeWithGrip" WindowButtonCommandsOverlayBehavior="Never" 
        RightWindowCommandsOverlayBehavior="Never" LeftWindowCommandsOverlayBehavior="Never">
    <controls:MetroWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/controls.buttons.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/controls.combobox.xaml" />
                <ResourceDictionary Source="/Resources/Icons.xaml" />
                <ResourceDictionary Source="/WPFStuff/Resources/BFF_ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:LongToCurrencyConverter x:Key="CurrencyConverter"/>
            <converters:CurrencyComboConverter x:Key="CurrencyComboConverter"/>
            <converters:DateComboConverter x:Key="DateComboConverter"/>
            <converters:AccountToTitViewModelConverter x:Key="AccountToTitViewModelConverter"/>
        </ResourceDictionary>
    </controls:MetroWindow.Resources>
    <controls:MetroWindow.Flyouts>
        <controls:FlyoutsControl>
            <controls:Flyout Name="SettingsFlyout" Header="{lex:Loc Settings}" Position="Right" Width="300">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <Label Content="{lex:Loc Theme}"/>
                        <ComboBox Name="ThemeCombo" SelectionChanged="ThemeCombo_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Stroke="{DynamicResource BlackBrush}" StrokeThickness="1" Fill="{Binding WhiteBrush}" Width="15" Height="15" />
                                        <Label Content="{Binding Path=Theme.Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Label Content="{lex:Loc Accents}"/>
                        <ComboBox Name="AccentCombo" SelectionChanged="AccentCombo_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Stroke="{DynamicResource BlackBrush}" StrokeThickness="1" Fill="{Binding AccentColorBrush}" Width="15" Height="15" />
                                        <Label Content="{Binding Path=Accent.Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Label Content="{lex:Loc Language}"/>
                        <ComboBox Name="LanguageCombo" SelectionChanged="LanguageCombo_SelectionChanged"/>
                        <Label Content="{lex:Loc Settings_Currency}"/>
                        <ComboBox Name="CurrencyCombo" SelectionChanged="CurrencyCombo_OnSelectionChanged" Style="{StaticResource VirtualisedMetroComboBox}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <Label Content="{Binding Name}" Grid.Row="0" Grid.Column="0" />
                                        <Label Content="{Binding DisplayName}" Grid.Row="0" Grid.Column="1" />
                                        <Label Content="{lex:Loc CurrencyCombo_Positive}" Grid.Row="1" Grid.Column="0" />
                                        <Label Content="{Binding Name, Converter={StaticResource CurrencyComboConverter}, ConverterParameter=positive}" Foreground="{StaticResource BrushPositive}" Grid.Row="1" Grid.Column="1" />
                                        <Label Content="{lex:Loc CurrencyCombo_Negative}" Grid.Row="2" Grid.Column="0" />
                                        <Label Content="{Binding Name, Converter={StaticResource CurrencyComboConverter}, ConverterParameter=negative}" Foreground="{StaticResource BrushNegative}" Grid.Row="2" Grid.Column="1" />
                                    </Grid>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Label Content="{lex:Loc Settings_Date}"/>
                        <ComboBox Name="DateCombo" SelectionChanged="DateCombo_OnSelectionChanged" Style="{StaticResource VirtualisedMetroComboBox}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <Label Content="{Binding Name}" Grid.Row="0" Grid.Column="0" />
                                        <Label Content="{Binding DisplayName}" Grid.Row="0" Grid.Column="1" />
                                        <Label Content="{lex:Loc DateCombo_Short}" Grid.Row="1" Grid.Column="0" />
                                        <Label Content="{Binding Name, Converter={StaticResource DateComboConverter}, ConverterParameter=short}"  Grid.Row="1" Grid.Column="1" />
                                        <Label Content="{lex:Loc DateCombo_Long}" Grid.Row="2" Grid.Column="0" />
                                        <Label Content="{Binding Name, Converter={StaticResource DateComboConverter}, ConverterParameter=long}"  Grid.Row="2" Grid.Column="1" />
                                    </Grid>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </controls:Flyout>
            <controls:Flyout Name="FileFlyout" Header="{lex:Loc File}" Position="Left">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <Button Content="{lex:Loc New}" Margin="10,5,10,5" Command="{Binding NewBudgetPlanCommand}" Click="Close_FileFlyout" Style="{DynamicResource SquareButtonStyle}" />
                        <Button Content="{lex:Loc Open}" Margin="10,5,10,5" Command="{Binding OpenBudgetPlanCommand}" Click="Close_FileFlyout" Style="{DynamicResource SquareButtonStyle}" />
                        <Button Content="{lex:Loc General_Import}" Margin="10,5,10,5" Click="Open_ImportDialog" Style="{DynamicResource SquareButtonStyle}" />
                    </StackPanel>
                </Grid>
            </controls:Flyout>
        </controls:FlyoutsControl>
    </controls:MetroWindow.Flyouts>
    <controls:MetroWindow.LeftWindowCommands>
        <controls:WindowCommands>
            <Button Name="FileButt" Content="{lex:Loc File}" Click="FileButt_Click" />
        </controls:WindowCommands>
    </controls:MetroWindow.LeftWindowCommands>
    <controls:MetroWindow.RightWindowCommands>
        <controls:WindowCommands>
            <Button Name="SettingsButt" Content="{lex:Loc Settings}" Click="SettingsButt_Click"/>
            <Button Content="{lex:Loc About}"/>
        </controls:WindowCommands>
    </controls:MetroWindow.RightWindowCommands>
    <Grid>
        <!-- todo: try Padding for spacing between the Headers, should be in MahApps.Metro 1.2.0 -->
        <!-- The Account-specific Tabs are added in the code behind through the DependencyProperty AllAcountsProperty.
             Sorry, I did not find another way for it. -->
        <controls:MetroTabControl Name="AccountsTabControl" Margin="10">
            <i:Interaction.Behaviors>
                <behaviors:AdditionalTabItemsBehavior ItemsSource="{Binding AllAccounts}">
                    <behaviors:AdditionalTabItemsBehavior.ContentTemplate>
                        <DataTemplate>
                            <usercontrols:TitDataGrid DataContext="{Binding Converter={StaticResource AccountToTitViewModelConverter}}" />
                        </DataTemplate>
                    </behaviors:AdditionalTabItemsBehavior.ContentTemplate>
                </behaviors:AdditionalTabItemsBehavior>
            </i:Interaction.Behaviors>
            <controls:MetroTabItem Header="{lex:Loc All_Accounts}">
                <usercontrols:TitDataGrid DataContext="{Binding AllAccountsViewModel}" />
            </controls:MetroTabItem>
            <controls:MetroTabItem Name="NewAccount_Tab"  Header="+">
                <StackPanel Width="500" DataContext="{Binding NewAccount}">
                    <Label Content="{lex:Loc NewAccount_Name}"/>
                    <TextBox Name="NewAccount_Name">
                        <TextBox.Text>
                            <Binding Path="Name" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <validationRules:NoEmptyNullStringRule />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Label Content="{lex:Loc NewAccount_StartingBalance}"/>
                    <TextBox MaxLength="30" Name="NewAccount_StartingBalance">
                        <TextBox.Text>
                            <Binding Path="StartingBalance" Mode="TwoWay" Converter="{StaticResource CurrencyConverter}" UpdateSourceTrigger="PropertyChanged" >
                                <Binding.ValidationRules>
                                    <validationRules:CurrencyRule />
                                    <validationRules:CurrencyLongRangeRule />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Button HorizontalAlignment="Right" Content="{lex:Loc General_Create}" Command="{Binding DataContext.NewAccountCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type controls:MetroWindow}}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MetroButton}">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Path=(Validation.HasError), ElementName=NewAccount_StartingBalance}" Value="False"/>
                                            <Condition Binding="{Binding Path=(Validation.HasError), ElementName=NewAccount_Name}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="IsEnabled" Value="True"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </controls:MetroTabItem>
        </controls:MetroTabControl>
    </Grid>
</controls:MetroWindow>
