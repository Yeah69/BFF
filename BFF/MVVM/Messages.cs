namespace BFF.MVVM
{
    public enum CultureMessage
    {
        Refresh,
        RefreshCurrency,
        RefreshDate
    }

    public enum SummaryAccountMessage
    {
        Refresh,
        RefreshTits,
        RefreshBalance,
        RefreshStartingBalance
    }
}
