<UserControl x:Class="BFF.MVVM.Views.NewFlagView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:BFF.MVVM.ViewModels"
             xmlns:views="clr-namespace:BFF.MVVM.Views"
             d:DataContext="{d:DesignInstance viewModels:INewFlagViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             Loaded="OnLoaded"
             mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="5,0,5,0"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"
                       Text="{lex:LocText NameOfNewFlag,
                                          Suffix=:}" />
            <TextBox x:Name="FocusMe"
                     Grid.Row="0"
                     Grid.Column="1"
                     MinWidth="50"
                     BorderBrush="{StaticResource AccentColorBrush}"
                     DockPanel.Dock="Right"
                     Focusable="True"
                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="5,0,5,0"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"
                       Text="{lex:LocText Color,
                                          Suffix=:}" />
            <Grid Grid.Row="1"
                  Grid.Column="1">
                <Popup x:Name="Popup"
                       StaysOpen="False">
                    <views:ColorPickerView Brush="{Binding Brush.Value, Mode=TwoWay}" />
                </Popup>

                <Button Click="Flag_OnClick">
                    <Button.Template>
                        <ControlTemplate>
                            <iconPacks:PackIconMaterial x:Name="PopHereUp"
                                                        Foreground="{Binding Brush.Value}"
                                                        Kind="{StaticResource FlagIcon}"
                                                        Style="{StaticResource CellIconMaterial}" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <Button Grid.Row="2"
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Click="AddButton_OnClick"
                    Command="{Binding AddCommand}"
                    ToolTip="{lex:Loc CreateNewFlag}">
                <iconPacks:PackIconMaterial Foreground="{DynamicResource BlackColorBrush}"
                                            Kind="{StaticResource AddIcon}"
                                            Style="{StaticResource CellIconMaterial}" />
            </Button>
        </Grid>
    </StackPanel>
</UserControl>
